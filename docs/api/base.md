# BasePlot

`BasePlot` is the abstract base class that provides all shared data-handling methods. You will not instantiate it directly, but its methods are inherited by both `ManhattanPlot` and `BoroughsPlot`.

::: neat_plots._base.BasePlot
